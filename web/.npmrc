# Build the production standalone bundle with a flat (npm-style)
# node_modules. pnpm's default symlink-based layout breaks when the
# standalone tree is copied with Windows tools that dereference
# symlinks — `node_modules/next` becomes a hard copy and Node loses
# the pnpm sibling-resolution chain for things like @next/env.
node-linker=hoisted
